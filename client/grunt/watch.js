module.exports = {
  options: {
    livereload: true,
  },
  assets: {
    files: ['src/**/*.{css,json,png,jpg,jpeg,gif,svg,eot,ttf,woff}']
  },
  module: {
    files: ['src/**/*.less', 'src/**/*.les', 'src/**/*.js', 'src/**/*.html']
  }
}